# 📄 Product Requirements Document (PRD)

## 프로젝트: (가칭) VisualLang - 그림으로 배우는 언어

### 1. 서비스 개요 (Overview)

**목표:**
사용자가 입력한 다국어 문장을 단순 번역에 그치지 않고, 직관적으로 이해하기 쉬운 '시각적 의미 단위(Visual Semantic Unit)'로 즉각 쪼개어, 각 단위에 맞는 생성형 이미지가 매칭된 플래시카드 형태로 제공하는 언어 학습 서비스.

**핵심 가치 (Core Value):**
사용자가 별도의 과정("그림 그려줘" 등의 뎁스) 없이, 문장 입력 한 번만으로 곧바로 한 편의 그림책 같은 맞춤형 언어 플래시카드를 제공받아 문맥을 시각적으로 연상하며 학습할 수 있도록 함.

---

### 2. 사용자 경험 흐름 (UX/UI Flow)

1. **입력:** 화면 하단(또는 중앙)의 텍스트 바에 학습하고 싶은 문장 혹은 텍스트를 입력하고 전송. (다국어 지원)
2. **비동기 로딩 (Skeleton UI):** 시스템 내부적으로 문장을 분석/분할하고 이미지 생성을 동시다발적으로 요청. (진행 단계를 세련된 애니메이션으로 표시)
3. **학습 (캐러셀 뷰):** 분해된 문구와 매칭된 이미지가 한 세트인 대형 플래시카드가 화면에 나타남. 사용자는 스와이프를 통해 장면에 따른 문장의 흐름을 시각적으로 연속해서 감상하고 학습.

---

### 3. 핵심 기능 (Core Features)

**3.1. 원스텝 파이프라인 (LLM + Image Gen 연속 호출)**

- 사용자의 입력을 받으면 LLM이 텍스트를 VSU(의미 단위)로 분할.
- "별도 확인 절차 없이", LLM이 도출한 n개의 단위 프롬프트를 바탕으로 백그라운드에서 즉각 다중 비동기 이미지 생성 호출(Promise.all 등)을 수행하여 대기 시간을 최소화.

**3.2. 반응형 플래시카드 캐러셀**

- 이미지와 텍스트(원문, 그리고 필요시 숨겨진/선택적 번역문)가 적절한 비율로 배치된 카드 슬라이드 UI 제공.

**3.3. 다국어 감지 및 대응**

- 입력된 문장이 영어, 일본어, 스페인어 등 어떤 언어든 감지하여 동일한 프로세스를 적용.

---

### 4. 부가/확장 기능 제안 (Suggested Features)

학습 효과와 서비스 체류 시간을 높이기 위해 아래와 같은 확장 기능을 도입하면 아주 훌륭한 서비스가 될 것입니다.

### 🎧 (1) 원어민 TTS (Text-to-Speech) 연동

플래시카드 내에 **[스피커 아이콘]**을 배치하거나 카드가 포커스 될 때 자동으로 (Auto-play) 해당 문장 조각을 원어민 발음으로 읽어줍니다. **시각(그림) + 청각(발음) + 텍스트**가 동시에 주어질 때 각인 효과가 가장 뛰어납니다.

### 🎨 (2) 학습 테마(화풍) 커스터마이징

설정 아이콘이나 입력창 옆에서 **이미지 그림체를 선택**할 수 있게 합니다. (예: `어린이 동화 스타일`, `스튜디오 지브리 스타일`, `미멀리즘 라인아트` 등).
실제 이미지 생성 API에 날릴 때 이 화풍을 Global Prompt로 묶어 넘김으로써, 4장의 플래시카드가 같은 동화책에서 나온 것처럼 이질감(Consistency 문제) 없이 생성되게 합니다.

### 💾 (3) 나만의 플래시카드 컬렉션 (단어장 보관함)

멋지게 생성된, 혹은 내가 자주 틀리는/외우고 싶은 카드 덱을 **'내 보관함'에 저장**하는 기능입니다. 나중에 다시 열어볼 수 있도록 하여 사용자의 리텐션(재방문율)을 끌어올립니다.

### 💡 (4) 호버/클릭 단어장 (Interactive Word Tooltip)

플래시카드 하단의 영어 문장을 볼 때, 모르는 영단어를 터치/호버하면 **동음이의어나 문맥상의 해석이 팝업**으로 뜨게 합니다.

### 🎲 (5) 그림 역순 퀴즈 (Active Recall Mode)

저장한 카드들을 복습할 때 쓸 수 있는 모드로, **스펠링이나 영문장을 가린 채 이미지만을 먼저 보여주며** "어떤 문장이었을까요?"를 먼저 떠올리게 하거나, 주요 단어를 빈칸 처리해 맞추게 하는 학습(Quiz) 기능입니다.

---

### 5. 아키텍처 및 퍼포먼스 고려사항 (Tech Considerations)

- **로딩 지연(Latency) 최적화 트릭:** 로딩 시간이 길어지면 사용자가 이탈합니다. LLM의 응답을 SSE(스트리밍) 형태로 받아, 문장의 첫 번째 조각이 분석되자마자 즉시 첫 번째 이미지를 생성 요청하고, 1번 카드를 렌더링하도록 프론트엔드와 최적화가 필요합니다.
- **안전/오류 핸들링:** 생성형 AI 특성상 일부 이미지가 생성 거부(NSFW 필터 등)될 수 있으므로, 이때 캐러셀 화면이 깨지지 않고 '기본 플레이스홀더 아이콘'과 텍스트로 대체 되도록 우아한 예외 처리(Graceful degradation)가 구축되어야 합니다.